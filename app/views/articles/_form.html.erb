<%= form_for(@article, class: "form-horizontal") do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

    <%= f.hidden_field :member_id %>

  <div class="form-horizontal" role="form">

    <div class="form-group">
    <%= f.label :title, class: "control-label sr-only" %>
        <div class="control-label">
          <%= f.text_field :title, class: "form-control",
              placeholder: "Title"  %>
        </div>
    </div>

    <div class="form-group">
    <%= f.label :category, class: "control-label sr-only" %>
        <div class="control-label">
          <%= f.text_field :category, class: "form-control",
              placeholder: "Category"  %>
        </div>
    </div>
    
    <div class="form-group">
        <%= f.label :article, class: "control-label sr-only" %>
        <!-- 写真挿入未実装 -->
        <div class="control-label">
          <%= f.text_area :article, class: "form-control", style: "width:100%;",
           rows: "20", placeholder: "Article" %>
        </div>
    </div>
    
    <div class="text-center">
      <div class="form-group">
        <%= f.submit class: "btn btn-primary" %>
      </div>
    </div>
  </div>


<% end %>
